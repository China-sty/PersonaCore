# PersonaCore — 多模态性格特质测评多智能体系统 · 实施计划

> 目标：通过**多模态线上测评**（文字 / 音频 / 视频）识别面试对象的性格特质，
> 最终用于**筛选目标人群**——在责任心、团队性、抗压与正向情绪等维度上做量化打分与排序。
> 采用**分阶段**推进，先用「文字对话识别性格特质」跑通 MVP，再逐步叠加音频、视频模态。

---

## 1. 目标与非目标

### 1.1 核心目标
- 用一组**多智能体（multi-agent）**协作完成一次结构化面试，并对候选人在若干性格维度上给出**有证据支撑的量化评分**。
- 支持三模态：文字（语义/内容）、音频（副语言/情绪）、视频（表情/微表情/视线/姿态）。
- 输出：维度得分 + 行为证据 + 可信度 + 筛选建议（通过/待定/淘汰），供 HR 做**人在环（human-in-the-loop）**决策。

### 1.2 非目标（当前版本不做）
- 不做完全自动的「一票否决」式招聘决策（AI 仅作为辅助，最终由人判定）。
- 不声称「科学心理测量」级别的诊断结论（不替代专业心理测评/临床诊断）。
- 第一阶段不做实时视频通话，先做「上传/回放」式的异步测评。

---

## 2. 测评维度与行为锚点

> 采用**大五人格（Big Five / OCEAN）**作为标准维度框架，用**行为锚点（behavioral anchors）**落地为可观察证据。
> 各维度的**企业要求（权重、合格线、是否一票否决）由管理员配置**，系统据此计算筛选结果。

| 维度 | 大五特质 | 正向行为锚点（举例） | 负向信号（举例） |
|------|---------|--------------------|-----------------|
| 尽责性 (C) | Conscientiousness | 计划性、承诺兑现、对细节负责、主动复盘 | 推卸责任、模糊承诺、无时间观念 |
| 宜人性 (A) | Agreeableness | 协作分工、倾听他人、冲突化解、共享荣誉 | 独占功劳、贬低同事、拒绝合作 |
| 情绪稳定性 (N↓) | Neuroticism（反向） | 压力下保持策略性、不情绪化、可恢复 | 崩溃、抱怨、归因外部、回避 |
| 外向性 (E) | Extraversion | 积极归因、主动表达、能量与主动性 | 持续负性、被动、回避社交 |
| 开放性 (O) | Openness | 接受新想法、好奇、适应变化、创新 | 僵化、抗拒变化、思维封闭 |

**维度配置（管理员可设）**：
- 每个维度：**权重**（默认各 20%）、**合格线/阈值**、可选**一票否决**；
- 正向/负向行为锚点可由管理员扩展，题库可按行业/岗位定制；
- 综合分 = Σ(维度分 × 权重)，再按阈值映射到「通过 / 待定 / 淘汰」。

**每个维度**需要定义：
1. 2~4 个子面向（facet）；
2. 每个子面向 3~5 道**结构化行为面试题**（STAR 框架）；
3. 1~5 分或 0~100 分的**评分量表（rubric）**与「证据标准」。

---

## 3. 系统总体架构

### 3.1 分层架构
```
┌──────────────────────────────────────────────────────────┐
│  接入层：Web / 小程序 / 视频会议   （面试进行与结果展示）      │
├──────────────────────────────────────────────────────────┤
│  编排层 Orchestrator：流程调度、状态机、会话管理、模态路由     │
├──────────────────────────────────────────────────────────┤
│  多智能体层 Agent Layer                                    │
│   ┌─ 面试官Agent ── 各维度分析师Agent ── 裁决Agent ─┐      │
│   │  (提问/追问)      (打分+取证)        (共识/冲突)  │      │
│   │  转写Agent        语音/表情信号Agent  报告Agent   │      │
│   └─────────────────────────────────────────────────┘      │
├──────────────────────────────────────────────────────────┤
│  能力层 Capability：LLM / ASR / 情感识别 / 表情识别 / 检索    │
├──────────────────────────────────────────────────────────┤
│  数据层：会话存储、证据标注、维度分数、模型评估、合规审计        │
└──────────────────────────────────────────────────────────┘
```

### 3.2 多智能体角色清单

| Agent | 职责 | 输入 | 输出 | 引入阶段 |
|-------|------|------|------|---------|
| **编排器 Orchestrator** | 控制面试流程、路由消息、维护状态、触发各 Agent、汇总结果 | 面试配置/会话状态 | 调度指令、最终汇总 | P1 |
| **面试官 Agent（可多专家）** | 主持结构化面试，按维度自适应提问与追问，控制节奏 | 题库 + 会话历史 | 下一条问题 / 追问 | P1 |
| **转写 Agent (ASR)** | 音频→文本，含说话人分离、时间戳 | 音频流/文件 | 转写文本 | P2 |
| **维度分析师 Agent**（每维度 1 个） | 基于文本证据对单一维度打分，引用原文支撑 | 转写文本 + rubric | 维度分 + 证据引用 | P1 |
| **语音信号 Agent** | 副语言特征：语调、语速、停顿/迟疑、音高、情绪唤醒度 | 音频特征 | 语音情绪/副语言信号 | P2 |
| **表情/微表情 Agent** | 面部动作单元(AU)、微表情、视线、姿态、头部动作 | 视频帧序列 | 表情/非言语信号 | P3 |
| **裁决/共识 Agent (Arbiter)** | 融合多源信号、消解分析器冲突、防止幻觉、给可信度 | 各分析器输出 | 融合分 + 冲突标注 | P1 |
| **反作弊 Agent** | 检测印象管理/念稿/前后矛盾/背题 | 全模态信号 | 作弊风险分 | P4 |
| **报告生成 Agent** | 生成结构化报告与筛选建议 | 汇总结果 | 报告 + 建议 | P1 |

### 3.3 智能体协作模式
- **编排式（Orchestration）**：主控 Agent 调度，各专家 Agent 单向产出（推荐起步，简单可控）。
- **黑板式（Blackboard / 共享记忆）**：各 Agent 读写共享会话与证据池，异步标注。
- **辩论/共识式（Debate / Consensus）**：维度分析师之间、分析师与裁决 Agent 之间对冲突证据做多轮辩论后收敛（P4 引入，提升鲁棒性）。
- **关键约束**：所有评分**必须引用原文证据**（grounded），禁止无证据打分；无证据 → 低可信度/不评分。

---

## 4. 分阶段实施路线图

### Phase 0 — 测评科学与合规准备（先行/贯穿）
**目标**：把「心理学 + 合规」地基打好，避免后期返工。
- [ ] 明确维度与子面向，产出《维度定义与行为锚点表》。
- [ ] 设计结构化题库（每维度 N 题 + 追问策略）。
- [ ] 制定评分 rubric 与证据标准（含「证据充分性」等级）。
- [ ] 数据合规：知情同意书、数据最小化、脱敏、留存与删除策略（对齐《个人信息保护法 PIPL》）。
- [ ] 建立**校准基准集**：邀请专家对若干样本做人工标注（golden set）。

### Phase 1 — 文字对话识别性格特质（MVP）✅ 优先
**目标**：纯文本完成一次完整面试 → 输出有证据的维度评分与报告。
- [ ] 编排器 + 会话状态机。
- [ ] 面试官 Agent（多维度自适应提问，STAR 追问）。
- [ ] 每维度 1 个分析师 Agent（引用原文打分）。
- [ ] 裁决 Agent（汇总、冲突消解、可信度）。
- [ ] 报告 Agent（结构化报告 + 初步筛选建议）。
- [ ] 评测：用 golden set 校准，计算与人工评分的相关性与一致性。
- **验收**：文字对话跑通「面试→评分→报告」闭环，评分含证据引用与可信度。

### Phase 2 — 音频模态（进行中）
**目标**：候选人可语音回答，用语音情绪信号增强判断。

**技术选型**：
- ASR：阿里云 **DashScope Paraformer**（云服务，HTTP API + `DASHSCOPE_API_KEY`）
- 情绪：**emotion2vec**（阿里 FunAudioLLM，从 ModelScope 下载）

**融入方式**（加「多模态输入层」，不改文字推理内核）：
```
音频 → ASR→文字 ┐
      └ 情绪→信号 ┴→ engine.send(文字, 信号)
```
- 情绪信号作为**中立事实**注入分析师 prompt（如「语音情绪：平静」），由 LLM 结合锚点解读，不直接打分。

**实施步骤**：
- [x] 2.1 接口骨架：`modalities/asr.py`、`emotion.py` + engine/web 支持音频 + 前端录音
- [ ] 2.1 联调 DashScope ASR（需 `DASHSCOPE_API_KEY`）
- [ ] 2.2 接入 emotion2vec 情绪识别，信号融合进评分
- [ ] 2.3 前端录音体验打磨
- **验收**：语音回答可识别，语音情绪作为辅助证据进入评分。

### Phase 3 — 视频模态
**目标**：接入视频，非言语信号 + 时间对齐。
- [ ] 人脸检测 + 动作单元(AU) + 表情/微表情识别（视线、姿态、头部动作）。
- [ ] 多模态**时间对齐**：文本/音频/视频同一时间轴，问题与反应对齐。
- [ ] 三模态融合（语义 + 副语言 + 非言语）。
- **验收**：视频面试跑通，融合输出维度评分，证据可回溯到具体时间戳。

### Phase 4 — 筛选产品化与鲁棒性
**目标**：面向「筛选目标人群」交付可上线系统。
- [ ] 反作弊 Agent（印象管理/念稿/矛盾检测）。
- [ ] 目标人群筛选：维度加权 → 综合评分 → 排序/阈值 → 通过/待定/淘汰建议。
- [ ] 可解释性：报告展示证据链与推理过程。
- [ ] 人在环复核工作流（HR 确认/驳回）。
- [ ] 校准与 A/B、偏见/公平性测试（不同性别/年龄/口音不产生系统性偏差）。
- [ ] 部署与监控（延迟、成本、质量监控 + 回归评测）。

---

## 5. 数据模型（草案）

```
Candidate（候选人）  id, 基本信息(最小化), 同意书状态
InterviewSession     id, candidate_id, 模态, 状态, 配置
Utterance            session_id, 角色, 文本, 音频URL, 视频URL, 起止时间戳
Evidence             utterance_id, 维度, 子面向, 引用片段, 方向(正/负), 强度
DimensionScore       session_id, 维度, 分数, 子面向分, 置信度, 证据数, 融合来源
SignalEvent          session_id, 模态, 时间戳, 信号类型, 值, 来源Agent
Report               session_id, 报告内容, 筛选建议, 版本, 审核状态
GoldenSample         用于校准的人工标注集
```

---

## 6. 技术栈建议

| 层 | 候选技术 |
|----|---------|
| 智能体/LLM | Claude（Sonnet/Opus 分级）、Claude Agent SDK / LangGraph / CrewAI 做编排 |
| 编排/状态 | LangGraph（图状态机）或自研编排器；会话状态存 Redis/DB |
| ASR | Whisper / FunASR / 阿里·腾讯云 ASR |
| 语音情绪 | wav2vec2 / openSMILE 特征 + 情绪分类模型 |
| 表情/微表情 | MediaPipe / OpenFace / RetinaFace + AU 检测 |
| 后端 | Python（FastAPI）为主；异步任务队列（Celery/Arq）处理长任务 |
| 前端 | Web（React）+ 视频通话 SDK（TRTC/Agora/自建 WebRTC） |
| 存储 | PostgreSQL（结构化）+ 对象存储（音视频文件） |
| 评测 | 自建评测集 + 打分一致性/相关性指标，CI 回归 |

---

## 7. 里程碑与时间（建议节奏，可调整）

| 阶段 | 主要交付物 | 建议周期 |
|------|-----------|---------|
| Phase 0 | 维度/题库/rubric/合规/校准集 | 与 P1 并行，1~2 周 |
| Phase 1 | 文字 MVP（面试→评分→报告闭环） | 2~4 周 |
| Phase 2 | 音频模态接入与融合 | 2~3 周 |
| Phase 3 | 视频模态接入与三模态融合 | 3~4 周 |
| Phase 4 | 筛选产品化 + 反作弊 + 上线 | 3~4 周 |

---

## 8. 风险与合规

- **伦理/合规（高风险）**：招聘评估涉及个人敏感信息，需 PIPL 合规（同意、最小化、可删除）、算法透明度、**人最终决策**、公平性测试；必要时算法备案评估。
- **有效性风险**：AI 评分需用 golden set 校准，避免「说得漂亮=高分」的表面效应；需与真实绩效数据回测。
- **作弊风险**：候选人印象管理/念稿/背题 → 引入反作弊 Agent 与多模态交叉验证。
- **幻觉风险**：评分必须 grounded 到原文，裁决 Agent 负责拦截无证据结论。
- **模态失效**：某模态缺失/质量差时降级（如无视频 → 仅文本+音频），并标注降级。

---

## 9. 目录结构（建议）

```
PersonaCore/
├─ plan.md                 # 本计划
├─ docs/                   # 维度定义、题库、rubric、合规文档
├─ agents/                 # 各智能体（interviewer/analyst/arbiter/report/...）
├─ orchestrator/           # 编排器与状态机
├─ modalities/
│  ├─ text/                # 文本处理
│  ├─ audio/               # ASR + 副语言
│  └─ video/               # 表情/微表情
├─ models/                 # 数据模型
├─ services/               # 业务服务、评测、报告
├─ eval/                   # 校准集、评测脚本、指标
├─ web/                    # 前端
└─ tests/                  # 单元/集成/回归
```

---

## 10. 下一步（立即开始）

1. 冻结**大五维度定义与行为锚点表**（Phase 0）。
2. 编写**结构化题库 + rubric**（覆盖 OCEAN 五个维度）。
3. 搭建 **Phase 1 MVP** 的最小闭环：面试官 Agent + 维度分析师 Agent + 裁决 Agent + 报告 Agent + 管理员维度配置。
4. 准备 golden set 用于校准与回归。

> 待确认：① 目标人群的具体画像/行业（影响题目与维度权重）；② 部署地区与合规要求；③ 是否接受先做「异步上传音视频」再过渡到实时。
