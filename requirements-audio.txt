# Phase 2 音频模态依赖（可选：未安装时系统仅支持文字面试）
# 安装： pip install -r requirements-audio.txt

# ASR（阿里云 DashScope Paraformer）
dashscope>=1.20.0

# 情绪识别（emotion2vec，从 ModelScope 下载）
funasr>=1.0.0
modelscope>=1.10.0
torch>=2.0.0
torchaudio>=2.0.0
librosa>=0.10.0
